# Data

DAFRL expects a MATLAB `.mat` file containing `GT`, `Mask`, and `Ref` arrays.
Dataset files are intentionally ignored by Git because they can be large or
subject to redistribution restrictions. Put a permitted dataset here and pass
it with `--data`.
